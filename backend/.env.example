# ── PostgreSQL Database ──────────────────────────
# Connect to a PostgreSQL database (local or cloud)
# Example: postgresql://user:password@localhost:5432/azure_pricing
DATABASE_URL=postgresql://user:password@localhost:5432/azure_pricing

# ── Server ─────────────────────────────────────
PORT=3001

# ── Sync Schedule ──────────────────────────────
# Cron expression: midnight every day
SYNC_CRON=0 0 * * *
